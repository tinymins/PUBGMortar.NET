<Window x:Class="PUBGMortar.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Left="10"
        Top="10">

    <Border Background="#E6FFD700"
            CornerRadius="8"
            Padding="16,10"
            BorderBrush="#FFFFA500"
            BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect Color="Black"
                              BlurRadius="10"
                              ShadowDepth="3"
                              Opacity="0.5"/>
        </Border.Effect>

        <TextBlock x:Name="MessageText"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#FF1A1A1A"
                   TextAlignment="Center"
                   FontFamily="Microsoft YaHei UI"/>
    </Border>
</Window>
